# Frontend 目录说明

前端项目建议统一放在 `frontend/` 下。

当前实际工程位于 `Frontend/metro-app`。  
若要完全统一结构，可在关闭开发服务器后执行迁移：

1. 将 `Frontend/metro-app` 移动到 `frontend/metro-app`
2. 确认无误后删除旧的 `Frontend/`

这样可避免课程项目后续出现“前后端目录命名不一致”的问题。
