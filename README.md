# 京轨（前端）


## 特性

-  **AI 对话集成**: 支持直接接入 OpenAI 兼容的模型接口，响应支持 Markdown 实时渲染。
-  **交互式地图**: 引入 Mapbox GL 渲染动态路线与地图。

## 本地启动

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

## 设置说明

在应用左下角的“设置”面板中，可以自行配置：
- **模型与接口**: 自定义 API Endpoint 和密钥。
- **通用设置**: 更改语言，以及填写渲染地图必须的 **Mapbox Token**。
